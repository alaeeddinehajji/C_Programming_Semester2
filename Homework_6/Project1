#include <stdlib.h>
#include <stdio.h>

int main(){
    /* //user enter a number and check if it's a prime number
    int number;
    printf("Check a number is a prime number: ");
    scanf("%d", &number);

    int temp = 0;
    for(int i = 1; i <= number; i++){
        if(number % i == 0)
           temp += 1;
    }
    if(temp == 2)
        printf("%d is a prime number", number);
    else
        printf("%d is not a prime number", number);
*/
    for(int number = 0; number <= 10000;  ++number){
    int temp = 0;
    for(int i = 1; i <= number; i++){
        if(number % i == 0)
           temp += 1;
        if(temp > 3)
           break;
    }
    if(temp == 2)
        printf("%d ,", number);
    }
    return 0;
}
